# 1. PUERTO OBLIGATORIO DE CLOUD RUN
server.port=${PORT:8080}

# 2. DATOS DE LA APLICACIÓN
spring.application.name=Biblioteca

# 3. CONEXIÓN TRADICIONAL POSTGRESQL
# Apunta al puerto local donde el Cloud SQL Proxy establece el túnel
spring.datasource.url=jdbc:postgresql://localhost:5432/biblioteca

# 4. CREDENCIALES: INSERTADAS DIRECTAMENTE EN EL CÓDIGO (HARDCODED)
spring.datasource.username=JuanDavid
spring.datasource.password=Juan1010962..

# 5. DRIVER Y DIALECTO
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect